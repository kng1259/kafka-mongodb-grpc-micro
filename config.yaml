server:
  port: 8080

kafka:
  brokers:
    - "kafka:9092"
  topic: "products"
  groupid: "product-consumer-group"

mongodb:
  uri: "mongodb://mongodb:27017"
  database: "productdb"
  collection: "products"

grpc:
  host: "consumer"
  port: 9080
  timeout: "10s"
